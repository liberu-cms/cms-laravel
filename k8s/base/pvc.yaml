apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cms-storage
  namespace: cms-laravel
  labels:
    app: cms-laravel
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
